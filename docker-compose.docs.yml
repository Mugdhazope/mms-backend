services:
  docs:
    image: mapmysutta_local_docs
    container_name: mapmysutta_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./mapmysutta:/app/mapmysutta:z
    ports:
      - '9000:9000'
    command: /start-docs
